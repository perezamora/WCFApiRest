# WCF REST API + Entity Framework

Simple Aplicacion REST con WCF en arquitectura N-Capas.

## Introducción

Simple Demo de una API-REST con WCF (Windows Communication Foundation) con persistencia en la capa DataAcces con entity Framework (sql server).
Se realiza los endpoints con protocolo HTTPrequest y todas las capas en la misma maquina.